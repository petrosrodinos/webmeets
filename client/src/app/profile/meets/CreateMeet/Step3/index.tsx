import { FC } from 'react';

interface Step3Props {
  register: any;
  errors: any;
  setValue: any;
  onNext: any;
}

const Step3: FC<Step3Props> = ({ register, errors, setValue, onNext }) => {
  return <div>step3</div>;
};

export default Step3;
