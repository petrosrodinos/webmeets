export enum BookingStatuses {
  PENDING = "pending",
  ACCEPTED = "accepted",
  REJECTED = "rejected",
  CANCELLED = "cancelled",
  COMPLETED = "completed",
}

export enum BookingActivityStatuses {
  CANCELLED = "cancelled",
  ACCEPTED = "accepted",
  REJECTED = "rejected",
}
