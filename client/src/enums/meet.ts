export enum MeetTypes {
  IN_PERSON = 'in-person',
  REMOTE = 'remote',
  CLIENTS_LOCATION = 'clients-location',
}

export enum VisibilityTypes {
  PUBLIC = 'public',
  PRIVATE = 'private',
  WITH_LINK = 'with-link',
  WITH_NFT = 'with-nft',
}
