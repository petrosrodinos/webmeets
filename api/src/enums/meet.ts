export enum MeetVisibilityTypes {
  PUBLIC = 'public',
  PRIVATE = 'private',
  WITH_LINK = 'with-link',
  WITH_NFT = 'with-nft',
}

export enum MeetType {
  REMOTE = 'remote',
  IN_PERSON = 'in-person',
  CLEINT_LOCATION = 'client-location',
}
